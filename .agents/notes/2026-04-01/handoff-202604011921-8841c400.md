---
created: 2026-04-01T23:21:40.313Z
type: handoff
session: 8841c400-264c-428b-a4cd-dd4b6c00d59d
---

# Handoff 2026-04-01 19:21

> Session `8841c400`

## Done

- **Crumbs rename shipped.** `tracks` → `crumbs` across package, docs, ADRs. Merged to main. CI green. Package is `@ontrails/crumbs`, types are `Crumb`/`CrumbSink`/`CrumbsLayer`, service is `crumbsService`, trails are `crumbs.status`/`crumbs.query`.
- **Config P2 fixes shipped.** NaN coercion, JSONC nested objects, configRef docs. Merged to main via PR #60.
- **Bootstrap --force fix shipped.** `ensure_project_deps` now respects the `FORCE` flag.
- **Full PR stack merged.** PRs #43, #50, #57, #58, #59, #60 all on main. CI green.
- **Design tenets written.** `.scratch/2026-03-31-design-principles.md` — canonical consolidation of all principles. Matt refined into the authoritative version now at `docs/tenets.md`.
- **10 draft ADRs updated.** Cross-cutting decisions propagated: `on` as authored default, lockfile as topo graph, crumbs rename, progressive event schemas, adapter-agnostic permits, pack-scoped events.
- **ADR shipping plan written.** `.scratch/2026-04-01-adr-shipping-plan.md` — tiered priority with dependency order.
- **Individual ADR Q&A completed.** Packs, parallel composition, visibility, webhooks, websockets, provisions, rig — all had questions answered and decisions locked.
- **ADR management skill created.** `.claude/skills/trails-adrs/` — conventions, templates, style guide (synthesized from ADR-000/001), script.
- **ADR management script built.** `.claude/skills/trails-adrs/scripts/adr.ts` — commands: create, promote, demote, update, fix, check, map. Preview-by-default with `--yes` to apply.
- **Maintainer agent created.** `.claude/agents/maintainer.md` — loads `trails-adrs` skill, ADR-lifecycle-specific memory, reads tenets first.
- **All 14 accepted ADRs cleaned up.** Frontmatter migrated (`author` → `owners`), numbered headings removed, NNN → NNNN file renaming via script, cross-references updated (90+ fixes), index rebuilt, decision-map.json generated.
- **Pedantic review completed.** Maintainer agents reviewed all 14 ADRs against full 14-point checklist. Fixed broken 6-digit links, added missing References, footnoted external claims, standardized Consequences subsections.
- **Inbound links fixed.** `docs/index.md`, `docs/vocabulary.md`, `docs/api-reference.md`, `docs/services.md` all updated to 4-digit ADR filenames.

## State

- All accepted ADRs (0000-0013) are clean and conformant
- Draft ADRs have `author` instead of `owners` (not yet migrated) and some from the 20260401 session have numbered titles
- The `adr.ts` script works but is a single 900+ line file — needs decomposition
- Inbound ref tracking added to decision map types but `promote` doesn't yet rewrite draft links to numbered links
- `docs/tenets.md` is the authoritative tenets doc (Matt's version, not `.scratch/`)
- Nothing is committed — all changes are local

## Next

- [ ] **Decompose `adr.ts` into modules.** Split into `lib/` with: `paths.ts`, `frontmatter.ts`, `discovery.ts`, `decision-map.ts`, `index.ts`, `references.ts`, `git.ts`. CLI entry point imports from lib.
- [ ] **Add promote-time link rewriting.** When a draft is promoted, find all `drafts/YYYYMMDD-slug.md` references across ADRs and docs, replace with `NNNN-slug.md`.
- [ ] **Scan non-ADR docs for inbound refs.** `findInboundRefs` currently only scans ADR files. Extend to `docs/*.md`, `plugin/`, `AGENTS.md` for the full reference graph.
- [ ] **Migrate draft frontmatter.** 17 drafts need `author` → `owners` and 4 from 20260401 need numbered titles removed.
- [ ] **Regenerate decision-map.json.** After script decomposition, run `map` to get inbound refs populated.
- [ ] **Commit the ADR cleanup.** Large diff — consider splitting: (1) frontmatter + heading fixes, (2) NNNN renumbering + cross-refs, (3) skill + script + maintainer agent.
- [ ] **Update `docs/tenets.md` to replace `.scratch/` version.** Matt has the authoritative version — the `.scratch/` design principles doc can be archived.
- [ ] **Begin Phase 1 ADR implementation.** Events runtime + triggers per the shipping plan.
