---
created: 2026-04-02T03:42:26.527Z
type: handoff
session: 8841c400-264c-428b-a4cd-dd4b6c00d59d
---

# Handoff 2026-04-02 23:42

> Session `8841c400`

## Done

- **ADR script decomposed.** `adr.ts` split into 8 modules (`lib/paths, frontmatter, discovery, decision-map, index, references, git, cli`). All commands work, lint clean.
- **Script features added.** Promote-time link rewriting, inbound ref scanning extended to `docs/*.md` and `.agents/notes/`, `depends_on` changed from `number[]` to `string[]` (supports draft slugs), `id`/`slug`/`title` frontmatter fields with alignment validation, `check --yes` auto-fix mode, title regex fix for draft format.
- **Decision maps split.** `docs/adr/decision-map.json` (accepted, stable) and `docs/adr/drafts/decision-map.json` (drafts, churns separately). Dropped `generated` timestamp for determinism. Identical output on re-run confirmed.
- **Drafts README generated.** `docs/adr/drafts/README.md` with month groups, linked titles, dependency sublists with resolved titles. Marked `<!-- Generated by adr.ts -->`.
- **All 31 ADR titles revised.** Formulaic em dash subtitles removed. Titles now make claims or paint pictures. Skill guidance updated with examples of good vs bad titles.
- **All 31 ADR slugs curated.** Slugs capture the decision, not just the topic. Surfaces explicitly named. `depends_on` refs updated when slugs changed.
- **`id`/`slug`/`title` backfilled** into all 31 ADR frontmatter. Markdownlint config updated (`MD025.front_matter_title: ""`) to handle frontmatter `title:` field.
- **Dependency audit completed.** Three parallel agents audited all 17 drafts against the shipping plan, tenets, and each other. Found 21 issues (7 high, 6 medium, 8 low).
- **7 contradictions resolved with Matt:**
  1. Webhooks wins on modeling (surface-level, not trail-level)
  2. Tracker is the foundational primitive — crumbs goes away, WebSocket replay is a cursor position
  3. `AuthCredentials` union drops (core doesn't know credential kinds)
  4. Search dependency chain corrected (entity depends on search, not reverse)
  5. Store needs service ID as first arg
  6. Triggers reference direction fixed
  7. Packs `pack()` originates here, not in config ADR
- **All 21 audit findings fixed.** `depends_on` populated for 16 drafts, 11 stale ADR numbers replaced, reference titles updated, substantive rewrites for Packs context, Inbound Event Surfaces webhook alignment, WebSocket replay model.
- **ADR check: 0 errors, 0 warnings** across all 31 files.

## State

- Branch `docs/adr-cleanup` has 3 commits (heavily amended), not pushed. 53 files changed.
- `package.json` has an unrelated `.try/*` workspace removal — not included in the branch.
- Resolutions doc: `.scratch/2026-04-01-contradiction-resolutions.md`
- Audit doc: `.scratch/2026-04-01-adr-dependency-audit.md`
- **Major pending decision: crumbs → tracker rename.** Decided in conversation but NOT yet executed. This is both a codebase rename (`@ontrails/crumbs` → `@ontrails/tracker`) and a conceptual elevation (observability feature → foundational primitive). ADR-0013 needs rewriting.

## Next

- [ ] **Rename crumbs → tracker** in codebase. Mechanical rename similar to tracks→crumbs (commit `14850f8`). Package, types, service, trails, tests, docs, ADRs.
- [ ] **Rewrite ADR-0013** from "Crumbs" to tracker as a foundational primitive. Not just a rename — the framing changes from "execution recording" to "sequenced log primitive that execution observation, events, and surfaces build on."
- [ ] **Update tenets and vocabulary** to reflect tracker terminology.
- [ ] **Add `store()` service ID** to store and drizzle draft API examples.
- [ ] **Push the branch** and create PR when ready.
- [ ] **Substance review pass** — deeper read of all draft ADR content for alignment with tenets (the structural/dependency pass is done; the content quality pass is next).
