---
created: 2026-03-27T22:17:51.094Z
type: handoff
session: 7813d309-89ef-4289-8c9c-1f1ecf5a2b97
---

# Handoff 2026-03-27 18:17

> Session `7813d309`

## Done

- **Docs rename complete.** All 33 doc/README files updated with new API names (topo, hike, testTrail, testHike, LogSink, etc.). Plan docs updated. Tone fixes in why-trails.md.
- **testHike implemented.** New `testHike()` in `@ontrails/testing` with follow tracking, expectFollowed, expectFollowedCount, injectFromExample. Tests passing.
- **ADR-001 updated.** Convention 3 now shows testTrail/testHike instead of testScenarios.
- **Code review pass complete.** 4 review agents + Codex reviewed the full codebase. All P1/P2 findings fixed:
  - Core: Result.toJson non-serializable guard, withTimeout mid-flight abort, redactor (sensitive non-strings, circular refs), fetch 4xx mapping, sortFields non-empty tuple, zodToJsonSchema callable defaults, safeStringify guard
  - MCP: progress unhandled rejections, internal trail filtering, blaze cleanup
  - Warden: single-line impl detection, off-by-one in returns-result, cached split, formatWardenReport clean run
  - Apps: hike() in scaffold, guide/survey output schema unions, add-trail ctx.cwd
- **CI governance app built.** `apps/ci` with `ci.warden` and `ci.drift` trails. GitHub Actions composite action. Local preview via `scripts/ci-check.sh`.
- **Warden formatters.** `formatGitHubAnnotations`, `formatJson`, `formatSummary` added to `@ontrails/warden`. `--format` flag on CLI warden trail.
- **Stack collapsed.** All stacked PRs folded into single `dev/v1` branch. Prior PRs closed.
- **Infra fixes.** bootstrap.sh, lefthook glob, esModuleInterop removal, CI bun-version-file + Turbo cache.
- **Warden dogfood → architectural discovery.** Attempted wrapping 16 rules as trails (842 LOC). Retro revealed half the rules were regex-parsing source to rediscover structured contract data — the anti-pattern Trails exists to eliminate. Pivoted to `validateTopo()` in core (~90 LOC) that queries contracts directly. Reverted 800 LOC of wrappers. Net -365 LOC, every retro caveat resolved. Full story in `.scratch/2026-03-27-warden-dogfood-assessment.md` and [gist](https://gist.github.com/galligan/c12382988531f524d44e965b5c75df86).
- **`validateTopo()` in core.** Structural contract validation: follows exist, no recursive cycles, examples parse, event origins valid. Returns `Result<void, ValidationError>` with structured issues.
- **`testAll()` in testing.** One-call governance: validateTopo + testExamples + testContracts + testDetours.

## State

- **Branch:** `dev/v1` on top of `main` (6 merged infra PRs already on main)
- **Build:** 37/37 turbo tasks green (typecheck, lint, test)
- **Not pushed yet.** All work is local. No open PR for `dev/v1`.
- **Warden findings in CI output:** 31 errors, 12 warnings — mostly from test fixtures. Warden needs `__tests__/` exclusion.
- **TRL-38 updated.** The warden dogfood Linear issue scope changed from "rules as trails" to the deeper architecture.

## Next

- [ ] Push `dev/v1` and open a PR against `main`
- [ ] Fix warden scanning test files — exclude `__tests__/` or add fixture-awareness
- [ ] Consider `oxc-parser` for remaining warden code convention rules (AST vs regex)
- [ ] Run another review loop on the full diff
- [ ] Set up changeset prerelease mode (`bunx changeset pre enter beta`) for `0.1.0-beta.N`
- [ ] Remove contract-duplicate rules from warden that `validateTopo` now covers (follows-trails-exist, no-recursive-follows, event-origins-exist, examples-match-schema, require-output-schema)
- [ ] Remaining API surface gaps: validateOutput, deriveFields rename, type utilities, schema accessors, @ontrails/essentials package
- [ ] `testTrail` kept (not renamed to testScenarios). ADR-001 updated to match.
