---
"@ontrails/trails": patch
"@ontrails/testing": patch
"@ontrails/warden": patch
---

Remove the retired `@ontrails/logging` workspace from the prerelease package set. Use `@ontrails/observe` for log and trace sink contracts and `@ontrails/observe/logtape` for LogTape forwarding.
