---
"@ontrails/commander": minor
"@ontrails/cli": minor
"@ontrails/oxlint-plugin": patch
"@ontrails/trails": patch
---

Add the dedicated `@ontrails/commander` adapter package and move the Commander runtime out of the `@ontrails/cli/commander` subpath. Extend the repo-local package-source guardrails to cover adapter package source as the Commander runtime moves under `adapters/`.
