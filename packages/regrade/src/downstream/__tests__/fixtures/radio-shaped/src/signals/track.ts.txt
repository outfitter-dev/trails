import { Result, trail } from '@ontrails/core';
import { z } from 'zod';

const loadTrack = trail('track.load', {
  implementation: () => Result.ok({ title: 'Lost Coast' }),
  output: z.object({ title: z.string() }),
});

export const playTrack = trail('play.track', {
  crosses: [loadTrack],
  input: crossInput,
  implementation: (input, ctx) => ctx.cross(loadTrack, input),
});
